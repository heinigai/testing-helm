apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "basesrv.fullname" . }}-test-connection"
  labels:
{{ include "basesrv.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "basesrv.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
