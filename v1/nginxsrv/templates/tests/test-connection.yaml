apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nginxsrv.fullname" . }}-test-connection"
  labels:
{{ include "nginxsrv.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "nginxsrv.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
