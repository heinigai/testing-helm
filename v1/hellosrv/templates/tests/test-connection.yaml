apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "hellosrv.fullname" . }}-test-connection"
  labels:
{{ include "hellosrv.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "hellosrv.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
