apiVersion: v1
kind: Service
metadata:
  name: {{ include "billtest.fullname" . }}
  labels:
    {{- include "billtest.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      nodePort: {{ .Values.service.nodePort }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "billtest.selectorLabels" . | nindent 4 }}
