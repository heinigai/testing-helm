apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "billtest.fullname" . }}-test-connection"
  labels:
{{ include "billtest.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "billtest.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
