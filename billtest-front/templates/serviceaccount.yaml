{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "billtest-front.serviceAccountName" . }}
  labels:
{{ include "billtest-front.labels" . | nindent 4 }}
{{- end -}}
