apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "frontlogsrv.fullname" . }}-test-connection"
  labels:
{{ include "frontlogsrv.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "frontlogsrv.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
