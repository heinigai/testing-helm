apiVersion: v1
kind: Service
metadata:
  name: {{ include "billtest-backend.fullname" . }}
  labels:
    {{- include "billtest-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      nodePort: {{ .Values.service.nodePort }}
      # targetPort: http
      protocol: TCP
      name: {{ .Values.service.name }}
  selector:
    {{- include "billtest-backend.selectorLabels" . | nindent 4 }}
