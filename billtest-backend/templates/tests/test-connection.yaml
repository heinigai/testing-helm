apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "billtest-backend.fullname" . }}-test-connection"
  labels:
{{ include "billtest-backend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "billtest-backend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
